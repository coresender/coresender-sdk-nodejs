export enum BodyType {
    HTML = 'html',
    TEXT = 'text',
}

export enum EmailItemStatus {
    ACCEPTED = 'accepted',
    REJECTED = 'rejected',
}

export enum ErrorCode {
    REQUIRED = 'REQUIRED',
    MAX = 'MAX',
    MIN = 'MIN',
    EMAIL = 'EMAIL',
}